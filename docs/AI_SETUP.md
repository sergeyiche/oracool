# 🤖 Настройка AI: RAG, DeepSeek и LLM модели

**Версия:** 1.0.0  
**Дата:** 2026-01-16

---

## 📋 Содержание

1. [Что такое RAG](#что-такое-rag)
2. [DeepSeek - рекомендуемая LLM](#deepseek)
3. [Сравнение моделей](#сравнение-моделей)
4. [Переключение моделей](#переключение-моделей)
5. [RAG vs Fine-tuning](#rag-vs-fine-tuning)

---

## 🔍 Что такое RAG

**RAG (Retrieval-Augmented Generation)** - архитектурный паттерн, который комбинирует:

1. **Retrieval** - поиск релевантной информации
2. **Augmentation** - добавление информации в контекст
3. **Generation** - генерация ответа с учётом контекста

### RAG пайплайн в проекте:

```
Вопрос → Embedding → Vector Search → Prompt + Context → LLM → Ответ
         (768 чисел)  (PostgreSQL)     (Augmentation)   (DeepSeek)
```

### Пример работы:

```
Вопрос: "Как найти смысл жизни?"
   ↓
Векторизация: [0.23, -0.45, ..., 0.12] (768 чисел)
   ↓
Поиск в БД (PostgreSQL + pgvector):
   • 87.2%: "Франкл: смысл не вообще, а в данный момент..."
   • 84.3%: "Смысл через ответственность за выборы..."
   • 81.7%: "Стоицизм: смысл в добродетели..."
   ↓
Промпт для LLM:
   Системный: "Ты философ-консультант..."
   Контекст: [3 найденные записи]
   Вопрос: "Как найти смысл жизни?"
   ↓
Ответ DeepSeek:
   "Вместо поиска абстрактного 'смысла жизни' попробуйте
   спросить себя: какой конкретный смысл я могу найти
   в своей текущей ситуации? Как говорил Франкл..."
```

### Компоненты:

**1. Embedding (векторизация)**
- Провайдер: Ollama
- Модель: `nomic-embed-text`
- Размерность: 768
- Стоимость: Бесплатно (локально)

**2. Vector Search (поиск)**
- База: PostgreSQL + pgvector
- Метрика: Косинусная близость
- Threshold: 0.65 (настраивается)

**3. LLM (генерация)**
- Рекомендуется: DeepSeek-V3
- Альтернатива: OpenAI GPT-4o-mini
- Локально: Ollama (бесплатно, но проблемы с русским)

---

## 🚀 DeepSeek - рекомендуемая LLM

### Почему DeepSeek?

✅ **В 10 раз дешевле OpenAI**  
✅ **Отличная поддержка русского языка**  
✅ **Качество как GPT-4**  
✅ **Быстрая генерация** (7-10 сек)  
✅ **Совместим с OpenAI API**  

### Стоимость:

| Использование | Стоимость |
|---------------|-----------|
| Один ответ | ~$0.0007 (~0.07₽) |
| 10 вопросов/день | ~$0.20/месяц (~20₽) |
| 100 вопросов/день | ~$2/месяц (~200₽) |

**Для сравнения:**
- DeepSeek: $2/месяц
- OpenAI GPT-4o-mini: $3-5/месяц
- OpenAI GPT-4: $50-100/месяц

### Быстрая настройка:

```bash
# 1. Получите ключ на https://platform.deepseek.com
# 2. Пополните баланс ($5-10)

# 3. Добавьте в .env
nano .env
# Найдите: DEEPSEEK_API_KEY=sk-your-api-key-here
# Замените на ваш ключ

# 4. Переключитесь на DeepSeek
/www/oracool/scripts/switch_llm.sh deepseek

# 5. Протестируйте
docker exec oracool-app php bin/console test:response YOUR_ID \
  "Как найти смысл жизни?"
```

**Ожидаемый результат:**
```
✅ LLM Model: deepseek:deepseek-chat
✅ Время: 7-10 секунд
✅ Релевантность: 85-90%
✅ Чистый русский язык
✅ Цитирует базу знаний (Франкла, Юнга и т.д.)
```

---

## 📊 Сравнение моделей

### Таблица провайдеров:

| Провайдер | Цена (вход/выход за 1M) | Качество | Русский | Скорость | Рекомендация |
|-----------|-------------------------|----------|---------|----------|--------------|
| **DeepSeek-V3** | $0.14 / $0.28 | ⭐⭐⭐⭐⭐ | ✅ Отлично | ⚡ 7-10 сек | ✅ **ЛУЧШИЙ** |
| **OpenAI GPT-4o-mini** | $0.15 / $0.60 | ⭐⭐⭐⭐⭐ | ✅ Отлично | ⚡ 5-8 сек | ✅ Альтернатива |
| **OpenAI GPT-4** | $5.00 / $15.00 | ⭐⭐⭐⭐⭐ | ✅ Отлично | 🐌 15-20 сек | 💰 Дорого |
| **Ollama Llama3.2** | 🆓 Бесплатно | ⭐⭐ | ⚠️ Смешанный | 🐌 15-20 сек | ⚠️ Для теста |
| **Ollama Qwen2.5** | 🆓 Бесплатно | ⭐⭐⭐ | ⚠️ Китайский | 🐌 15-20 сек | ⚠️ Для теста |
| **Ollama Mistral** | 🆓 Бесплатно | ⭐⭐⭐ | ⚠️ Средне | 🐌 15-20 сек | 🤔 Попробовать |

### Примеры проблем с Ollama:

**Llama3.2:**
```
"Смысл жизни не есть что-то externальное, но скорее 
Internalizированная цель..."
```
❌ Проблемы: `externальное`, `Internalizированная`

**Qwen2.5:**
```
"Попробуйте задать себе вопрос: что я могу 可控 в своей жизни?"
```
❌ Проблемы: китайские символы `可控`

**DeepSeek:**
```
"Вместо поиска абстрактного 'смысла жизни' попробуйте спросить себя: 
какой конкретный смысл я могу найти в своей текущей ситуации?"
```
✅ Чистый русский!

---

## 🔄 Переключение моделей

### Использование скрипта `switch_llm.sh`:

```bash
# DeepSeek (рекомендуется)
/www/oracool/scripts/switch_llm.sh deepseek

# OpenAI GPT-4o-mini
/www/oracool/scripts/switch_llm.sh openai

# Ollama Llama3.2 (для разработки)
/www/oracool/scripts/switch_llm.sh llama

# Ollama Mistral
/www/oracool/scripts/switch_llm.sh mistral

# Ollama Qwen2.5
/www/oracool/scripts/switch_llm.sh qwen
```

**Скрипт автоматически:**
1. ✅ Обновляет `.env`
2. ✅ Обновляет `config/services.yaml`
3. ✅ Перезапускает контейнеры
4. ✅ Очищает кэш Symfony
5. ✅ Показывает команду для теста

### Ручное переключение (если нужно):

```bash
# 1. Отредактируйте .env
nano .env

# Измените провайдера:
LLM_PROVIDER=deepseek  # или: openai, ollama

# 2. Отредактируйте config/services.yaml
nano config/services.yaml

# Найдите и измените alias:
App\Core\Port\LLMServiceInterface:
    alias: 'llm_service.deepseek'  # или: openai, ollama

# 3. Перезапустите
docker compose restart app messenger-consumer
docker exec oracool-app php bin/console cache:clear
```

### Проверка текущей модели:

```bash
# Через тест
docker exec oracool-app php bin/console test:response YOUR_ID \
  "тест"

# Смотрите в выводе:
# LLM Model: deepseek:deepseek-chat  ← текущая модель
```

---

## 🆚 RAG vs Fine-tuning

### Вопрос: "Можно ли дообучить (fine-tune) DeepSeek?"

**Ответ:**
- ⚠️ **Fine-tuning через DeepSeek API** - НЕТ (нужен доступ к весам модели)
- ✅ **RAG (что у вас)** - ДА! И это **лучший** выбор для вашего случая

### Сравнение подходов:

| Аспект | Fine-tuning | RAG (ваш проект) |
|--------|-------------|------------------|
| **Что это** | Переобучение модели | Добавление контекста в промпт |
| **Меняет модель** | ✅ Да | ❌ Нет |
| **Нужен доступ к весам** | ✅ Да | ❌ Нет |
| **Время настройки** | 🐌 Часы/дни | ⚡ Секунды |
| **Обновление данных** | 🐌 Переобучать | ⚡ Добавить в БД |
| **Стоимость** | 💰💰 Дорого (GPU) | 💰 Дешево (только API) |
| **Гибкость** | ⚠️ Низкая | ✅ Высокая |
| **Персонализация** | ✅ На уровне модели | ✅ На уровне пользователя |

### Когда нужен Fine-tuning?

Fine-tuning имеет смысл если:
- 📚 Огромный корпус (миллионы слов специальных знаний)
- 🎨 Специфический стиль (писать как Достоевский)
- 💾 Встроить знания в модель (но сложно обновлять)
- 🖥️ Есть GPU и время на обучение

### Почему RAG идеален для философского бота:

✅ **Легко обновлять знания:**
```bash
docker exec oracool-app php bin/console knowledge:import \
  /path/to/new_philosophy.txt YOUR_ID manual
```

✅ **Персонализация на пользователя:**  
Каждый пользователь имеет свою базу знаний

✅ **Прозрачность:**  
Видно какой контекст используется (relevance scores)

✅ **Быстро:**  
Добавить знания = 1 команда, 10 секунд

✅ **Дешево:**  
Платите только за API запросы (~$2/месяц)

---

## 🔧 Настройка RAG параметров

### Релевантность (threshold):

```bash
# Снизить threshold (больше контекста, но менее релевантного)
docker exec oracool-app php bin/console profile:update \
  YOUR_ID --threshold 0.6

# Повысить threshold (меньше контекста, но более релевантного)
docker exec oracool-app php bin/console profile:update \
  YOUR_ID --threshold 0.75

# По умолчанию: 0.65 (рекомендуется)
```

**Когда менять:**
- Если релевантность < 70% → снизить threshold до 0.6
- Если много нерелевантного → повысить до 0.7-0.75

### Стиль общения:

```bash
# Креативный (больше метафор, поэтичность)
docker exec oracool-app php bin/console profile:update \
  YOUR_ID --style creative

# Формальный (философская глубина, уважительно)
docker exec oracool-app php bin/console profile:update \
  YOUR_ID --style formal

# Сбалансированный (глубина + простота)
docker exec oracool-app php bin/console profile:update \
  YOUR_ID --style balanced  # по умолчанию
```

---

## 📊 Мониторинг AI качества

### Ключевые метрики:

```bash
# Тест RAG (только поиск)
docker exec oracool-app php bin/console test:rag YOUR_ID \
  "ваш вопрос"

# Смотрите:
# - Релевантность топ записи (должно быть > 70%)
# - Количество найденных записей (должно быть 3-5)
```

**Хорошие показатели:**
- ✅ Релевантность: 75-90%
- ✅ Найдено: 3-5 записей
- ✅ Топ запись > 80%

**Плохие показатели:**
- ⚠️ Релевантность < 60%
- ⚠️ Найдено < 2 записей
- ❌ Топ запись < 50%

**Решения:**
1. Пополните базу знаний (см. `docs/TRAINING.md`)
2. Снизьте threshold до 0.6
3. Проверьте что embedding работает

---

## 🐛 Troubleshooting AI

### Проблема: "Insufficient Balance" (DeepSeek)

```bash
# Баланс закончился
# Решение:
# 1. Зайдите на https://platform.deepseek.com
# 2. Пополните баланс ($5-10)
# 3. Попробуйте снова
```

### Проблема: Ответы с английскими/китайскими словами

```bash
# Используете Ollama?
# Решение: переключитесь на DeepSeek или OpenAI

/www/oracool/scripts/switch_llm.sh deepseek
```

### Проблема: Низкая релевантность (< 60%)

```bash
# Причины:
# 1. Мало знаний в базе
# 2. Знания не релевантны вопросу
# 3. Высокий threshold

# Решения:
# 1. Добавьте больше знаний (см. docs/TRAINING.md)
# 2. Снизьте threshold
docker exec oracool-app php bin/console profile:update \
  YOUR_ID --threshold 0.6
```

### Проблема: Ollama не отвечает

```bash
# Проверьте что контейнер запущен
docker compose ps ollama

# Проверьте логи
docker logs oracool-ollama -f

# Проверьте API
docker exec oracool-app curl http://ollama:11434/api/tags

# Перезапустите
docker compose restart ollama

# Скачайте модель заново
docker exec oracool-ollama ollama pull nomic-embed-text
```

---

## 📚 Дополнительно

- `docs/DEPLOYMENT.md` - Развертывание и настройка
- `docs/TRAINING.md` - Обучение и пополнение базы знаний
- `README.md` - Обзор проекта

---

**Версия:** 1.0.0  
**Дата:** 2026-01-16  
**Статус:** ✅ Production Ready
