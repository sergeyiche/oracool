# Анализ проекта и план реализации

## 📋 Обзор проекта

Проект представляет собой платформу для работы ИИ-агентов в мессенджерах. Первый агент - "Оракул" - должен быть реализован как "цифровой двойник" пользователя, который учится на его стиле общения и отвечает от его имени.

## 🔍 Выявленные противоречия и проблемы

### 1. Концептуальное противоречие

**Проблема:** 
- `Oracle.md` описывает агента как "цифровой двойник", который:
  - Имитирует стиль пользователя
  - Учится на его сообщениях
  - Использует RAG с базой знаний пользователя
  - Имеет систему обратной связи (одобрение/исправление/удаление)

- Код в файлах (`PhilosophicalAIService.php`, `OracleResponse.php`, `PhilosophicalProfile.php`) реализует:
  - Философский оракул с стоической мудростью
  - Когнитивные искажения и рефрейминг
  - Добродетели и упражнения
  - Притчи и сократические вопросы

**Решение:** 
- Оракул должен быть "цифровым двойником" согласно `Oracle.md`
- Философские функции могут быть:
  - Опциональным модулем/плагином
  - Отдельным агентом типа "PhilosophyBot"
  - Или интегрированы как часть базы знаний пользователя (если пользователь интересуется философией)

### 2. Архитектурные несоответствия

**Проблемы:**
- В коде используется `PhilosophicalProfile` и философские доменные модели, но они не соответствуют концепции "цифрового двойника"
- Отсутствует система эмбеддингов и векторного поиска (ключевая часть из `Oracle.md`)
- Нет реализации базы знаний (knowledge base) для хранения стиля пользователя
- Отсутствует система обратной связи (кнопки одобрения/исправления)

**Решение:**
- Создать отдельные доменные модели для "цифрового двойника":
  - `UserProfile` (стиль, интересы, примеры ответов)
  - `KnowledgeBase` (векторизованные данные)
  - `FeedbackSystem` (одобрения/исправления)
- Философские модели оставить как опциональный модуль

### 3. Отсутствующие компоненты

**Критически важные компоненты из `Oracle.md`:**
1. ✅ Система эмбеддингов (отсутствует)
2. ✅ Векторная база данных (pgvector или альтернатива)
3. ✅ База знаний пользователя (knowledge_base)
4. ✅ Система релевантности (косинусная близость)
5. ✅ Режимы работы бота (silent/active/aggressive)
6. ✅ Система обратной связи (кнопки одобрения/исправления)
7. ✅ Проактивные сообщения (подкидывание тем)
8. ✅ Команда `/train_from_chat`

**Существующие компоненты:**
- ✅ Архитектура адаптеров (Telegram)
- ✅ Очередь сообщений (Messenger)
- ✅ База данных (PostgreSQL)
- ✅ Use Cases структура

### 4. Проблемы в коде

**Namespace и структура:**
- Некоторые классы используют неправильные namespace (нужно проверить все файлы)
- Отсутствуют интерфейсы (Ports) для зависимостей
- Нет реализации репозиториев

**База данных:**
- `sql.sql` содержит базовую схему, но нет таблиц для:
  - Векторных эмбеддингов
  - Базы знаний (knowledge_base)
  - Обратной связи (feedback)
  - Режимов работы бота (bot_modes)
  - Белого списка чатов (whitelist)

**Конфигурация:**
- `oracle_commands.yaml` содержит команды для философского оракула, но нужны команды для управления "цифровым двойником"

## 📐 Рекомендуемая архитектура

### Платформа (Platform Layer)

```
┌─────────────────────────────────────────────────────────┐
│              Платформа ИИ-агентов                        │
├─────────────────────────────────────────────────────────┤
│  • Управление агентами (Agent Registry)                 │
│  • Общие адаптеры (Telegram, WhatsApp, etc.)           │
│  • Очередь сообщений (Message Queue)                    │
│  • База данных (PostgreSQL + pgvector)                  │
│  • Кеш (Redis)                                          │
│  • Логирование и мониторинг                             │
└─────────────────────────────────────────────────────────┘
```

### Агент Оракул (Oracle Agent)

```
┌─────────────────────────────────────────────────────────┐
│              Агент "Оракул" (Цифровой двойник)          │
├─────────────────────────────────────────────────────────┤
│  Доменные модели:                                        │
│  • UserProfile (стиль, интересы, примеры)               │
│  • KnowledgeBase (векторизованные данные)               │
│  • ConversationContext (контекст диалога)               │
│  • Feedback (одобрения/исправления)                     │
│                                                          │
│  Use Cases:                                              │
│  • ProcessIncomingMessage (фильтрация и релевантность) │
│  • GenerateResponse (RAG + LLM)                         │
│  • HandleFeedback (обучение на коррекциях)              │
│  • ProactiveMessaging (подкидывание тем)                 │
│  • TrainFromChat (обучение из истории)                   │
│                                                          │
│  Адаптеры:                                               │
│  • EmbeddingService (векторизация)                      │
│  • VectorSearchService (поиск по эмбеддингам)           │
│  • LLMService (генерация ответов)                       │
│  • FeedbackUI (кнопки в Telegram)                       │
└─────────────────────────────────────────────────────────┘
```

## 🚀 План реализации

### Этап 1: Базовая инфраструктура платформы (1-2 недели)

#### 1.1 Настройка проекта
- [ ] Инициализация Symfony проекта (если еще не сделано)
- [ ] Настройка `composer.json` с зависимостями:
  - Symfony компоненты
  - Doctrine ORM
  - Symfony Messenger
  - Redis клиент
  - PostgreSQL драйвер
- [ ] Настройка `.env` файла с переменными окружения
- [ ] Создание `.env.example` без секретов

#### 1.2 База данных
- [ ] Установка PostgreSQL с расширением `pgvector`
- [ ] Создание базовой схемы (users, conversations, messages)
- [ ] Создание таблиц для платформы:
  - `agents` (регистр агентов)
  - `agent_configs` (конфигурации агентов)
  - `bot_modes` (режимы работы ботов)
  - `whitelisted_chats` (белый список чатов)
- [ ] Создание миграций Doctrine

#### 1.3 Адаптеры мессенджеров
- [ ] Реализация `TelegramWebhookController`
- [ ] Валидация секретного токена Telegram
- [ ] Маппинг Telegram формата в доменную модель
- [ ] Асинхронная обработка через Messenger
- [ ] Обработка ошибок и логирование

#### 1.4 Очередь сообщений
- [ ] Настройка Symfony Messenger
- [ ] Создание очередей:
  - `message_processing` (основная)
  - `ai_processing` (для ИИ)
  - `scheduled_processing` (для проактивных сообщений)
- [ ] Реализация workers
- [ ] Настройка retry механизма

### Этап 2: Реализация агента Оракул - MVP (2-3 недели)

#### 2.1 Доменные модели
- [ ] `UserProfile` - профиль пользователя:
  - Стиль общения (краткий/развернутый, смайлы/без)
  - Ключевые интересы
  - Примеры ответов
  - Настройки бота (режим работы, порог релевантности)
- [ ] `KnowledgeBaseEntry` - запись в базе знаний:
  - Текст
  - Вектор эмбеддинга
  - Метаданные (источник, дата, теги)
- [ ] `ConversationContext` - контекст диалога:
  - История сообщений
  - Текущая тема
  - Состояние диалога
- [ ] `Feedback` - обратная связь:
  - Тип (одобрение/исправление/удаление)
  - Исходный ответ бота
  - Исправленный вариант (если есть)
  - Дата и время

#### 2.2 Система эмбеддингов
- [ ] Выбор провайдера эмбеддингов:
  - Вариант 1: OpenAI API (text-embedding-ada-002 или новее)
  - Вариант 2: OpenRouter (бесплатные модели)
  - Вариант 3: Локально через Ollama (nomic-embed-text)
- [ ] Реализация `EmbeddingService` интерфейса
- [ ] Реализация конкретных адаптеров для провайдеров
- [ ] Кеширование эмбеддингов (Redis)

#### 2.3 Векторный поиск
- [ ] Настройка pgvector в PostgreSQL
- [ ] Создание таблицы `knowledge_base` с полем `embedding vector(1536)`
- [ ] Реализация `VectorSearchService`:
  - Метод поиска по косинусной близости
  - Настройка порога релевантности (по умолчанию 0.7)
  - Возврат топ-N результатов
- [ ] Индексы для производительности

#### 2.4 База знаний
- [ ] Реализация `KnowledgeBaseRepository`
- [ ] Методы:
  - `addEntry()` - добавление записи с автоматической векторизацией
  - `findRelevant()` - поиск релевантных записей
  - `bulkImport()` - массовый импорт из истории чата
- [ ] Инициализация базы знаний:
  - Создание `knowledge_base.txt` с контекстным портретом
  - Векторизация и загрузка в БД

#### 2.5 Определение релевантности
- [ ] Реализация `RelevanceChecker`:
  - Векторизация входящего сообщения
  - Поиск в базе знаний
  - Проверка порога релевантности
  - Учет контекста диалога
- [ ] Настройка порога через конфигурацию пользователя

### Этап 3: Генерация ответов (RAG) (1-2 недели)

#### 3.1 RAG система
- [ ] Реализация `ResponseGenerator`:
  - Получение релевантных фрагментов из базы знаний
  - Формирование промпта для LLM
  - Вызов LLM API
  - Постобработка ответа
- [ ] Промпт-шаблон:
  ```
  Ответь на сообщение '{message}' так, как ответил бы человек 
  со следующими знаниями и стилем: {knowledge_fragments}
  
  Стиль пользователя:
  - {style_description}
  - Примеры: {example_responses}
  
  Ответ должен быть кратким и естественным.
  ```

#### 3.2 LLM интеграция
- [ ] Выбор LLM провайдера:
  - Вариант 1: Локально через Ollama (llama3.2, mistral)
  - Вариант 2: DeepSeek API (недорогой)
  - Вариант 3: Gemini API
- [ ] Реализация `LLMService` интерфейса
- [ ] Реализация конкретных адаптеров
- [ ] Обработка ошибок и fallback

#### 3.3 Контекст диалога
- [ ] Реализация `ContextManager`:
  - Хранение истории диалога (Redis)
  - Учет последних N сообщений при генерации
  - Определение текущей темы
- [ ] Интеграция с генерацией ответов

### Этап 4: Система обратной связи и обучения (1-2 недели)

#### 4.1 UI обратной связи
- [ ] Реализация кнопок в Telegram:
  - "✅ Одобрить"
  - "✏️ Исправить"
  - "❌ Удалить"
- [ ] Обработка callback'ов от кнопок
- [ ] Приватный чат-лог для владельца бота

#### 4.2 Обучение на коррекциях
- [ ] Реализация `FeedbackHandler`:
  - При одобрении: добавление ответа в базу знаний
  - При исправлении: сохранение исправленного варианта в базу знаний
  - При удалении: добавление в "черный список" шаблонов
- [ ] Автоматическая векторизация одобренных/исправленных ответов
- [ ] Обновление профиля пользователя на основе обратной связи

#### 4.3 Команды управления
- [ ] `/bot_mode silent` - только логирование
- [ ] `/bot_mode active` - отвечать на релевантное
- [ ] `/bot_mode aggressive` - активно участвовать
- [ ] `/train_from_chat [chat_id]` - обучение из истории
- [ ] `/set_relevance_threshold [0.0-1.0]` - настройка порога
- [ ] `/status` - статус бота и статистика

### Этап 5: Проактивные сообщения (1 неделя)

#### 5.1 Система проактивности
- [ ] Реализация `ProactiveMessagingService`:
  - Отслеживание времени последнего сообщения в чате
  - Выбор случайной темы из интересов пользователя
  - Генерация проактивного сообщения
- [ ] Настройка интервала (по умолчанию 30 минут)
- [ ] Учет режима работы бота (только в aggressive)

#### 5.2 Планировщик задач
- [ ] Настройка Symfony Scheduler или cron
- [ ] Периодическая проверка чатов
- [ ] Отправка проактивных сообщений

### Этап 6: Безопасность и развертывание (1 неделя)

#### 6.1 Безопасность
- [ ] Белый список доверенных чатов
- [ ] Валидация всех входящих данных
- [ ] Защита от спама (rate limiting)
- [ ] Шифрование чувствительных данных
- [ ] Аудит логов

#### 6.2 Развертывание
- [ ] Настройка VPS или облачного сервиса
- [ ] Docker контейнеризация (опционально)
- [ ] Настройка CI/CD
- [ ] Мониторинг и алерты
- [ ] Резервное копирование БД

### Этап 7: Тестирование и донастройка (ongoing)

#### 7.1 Фаза 0: Тень (1-2 недели)
- [ ] Запуск в режиме `silent`
- [ ] Сбор реальных данных
- [ ] Анализ входящих сообщений
- [ ] Пополнение базы знаний

#### 7.2 Фаза 1: Наблюдатель (1 неделя)
- [ ] Включение проактивности
- [ ] Наблюдение за реакциями
- [ ] Корректировка тем и стиля

#### 7.3 Фаза 2: Участник (2-3 недели)
- [ ] Включение ответов на релевантное (высокий порог 0.8-0.9)
- [ ] Активное использование обратной связи
- [ ] Обучение на коррекциях
- [ ] Анализ качества ответов

#### 7.4 Фаза 3: Ассистент (ongoing)
- [ ] Постепенное снижение порога релевантности
- [ ] Улучшение качества на основе обратной связи
- [ ] Оптимизация промптов
- [ ] Расширение базы знаний

## 📁 Структура проекта (финальная)

```
oracle/
├── config/
│   ├── packages/
│   │   ├── doctrine.yaml
│   │   ├── messenger.yaml
│   │   ├── monolog.yaml
│   │   └── telegram.yaml
│   └── routes/
│       └── webhook.yaml
├── docs/
│   ├── Oracle.md
│   ├── Agents.md
│   └── ANALYSIS_AND_PLAN.md
├── migrations/
│   ├── sql.sql
│   └── Version*.php
├── scripts/
│   └── setup_webhook.sh
├── src/
│   ├── Adapter/
│   │   ├── AI/
│   │   │   ├── EmbeddingServiceInterface.php
│   │   │   ├── OpenAIEmbeddingService.php
│   │   │   ├── OllamaEmbeddingService.php
│   │   │   ├── LLMServiceInterface.php
│   │   │   ├── OllamaLLMService.php
│   │   │   └── DeepSeekLLMService.php
│   │   └── Telegram/
│   │       ├── TelegramWebhookController.php
│   │       ├── TelegramMessageMapper.php
│   │       └── TelegramFeedbackUI.php
│   ├── Core/
│   │   ├── Domain/
│   │   │   ├── Message/
│   │   │   │   └── IncomingMessage.php
│   │   │   ├── User/
│   │   │   │   ├── User.php
│   │   │   │   └── UserProfile.php
│   │   │   ├── KnowledgeBase/
│   │   │   │   └── KnowledgeBaseEntry.php
│   │   │   └── Feedback/
│   │   │       └── Feedback.php
│   │   ├── UseCase/
│   │   │   ├── ProcessIncomingMessage.php
│   │   │   ├── CheckRelevance.php
│   │   │   ├── GenerateResponse.php
│   │   │   ├── HandleFeedback.php
│   │   │   ├── TrainFromChat.php
│   │   │   └── SendProactiveMessage.php
│   │   └── Port/
│   │       ├── EmbeddingServiceInterface.php
│   │       ├── VectorSearchInterface.php
│   │       ├── LLMServiceInterface.php
│   │       └── KnowledgeBaseRepositoryInterface.php
│   └── Worker/
│       └── MessageProcessingWorker.php
└── tests/
```

## 🔧 Технический стек

- **Framework:** Symfony 6.x
- **ORM:** Doctrine
- **Database:** PostgreSQL 14+ с pgvector
- **Cache:** Redis
- **Message Queue:** Symfony Messenger
- **Embeddings:** OpenAI / Ollama / OpenRouter
- **LLM:** Ollama (локально) / DeepSeek / Gemini
- **Messenger:** Telegram Bot API

## ⚠️ Важные замечания

1. **Консервативность:** Бот должен быть консервативным - лучше промолчать, чем сказать глупость
2. **Обратная связь критична:** Первые недели обратной связи формируют фундамент поведения бота
3. **Постепенное обучение:** Не спешите с ответами, сначала соберите данные
4. **Безопасность:** Никогда не коммитьте токены, используйте переменные окружения
5. **Масштабируемость:** Проектируйте с учетом горизонтального масштабирования

## 📝 Следующие шаги

1. ✅ Файлы организованы по директориям
2. ✅ Анализ противоречий выполнен
3. ✅ План реализации создан
4. ⏭️ Начать с Этапа 1: Базовая инфраструктура

---

**Дата создания:** 2026-01-12  
**Версия:** 1.0
